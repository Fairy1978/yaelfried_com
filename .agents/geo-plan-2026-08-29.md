# GEO plan for the clinic
Written 2026-08-29. Baseline: `.agents/conclusions-2026-07-27.md`.
State below was re-verified against the working tree on 2026-08-29, not carried over on trust.

## Already done (committed)

- Real headings on all 12 pages (`4585ce1`), plus the scroll-script repair it broke (`4d7877b`)
- Contact form labels and image alt text (`4585ce1`)
- `sameAs` social links in the schema on all 12 pages
- Unique title and description per page, sitemap, robots.txt, lang attributes (`51ed3ae`)
- AI crawlers confirmed unblocked: GPTBot, PerplexityBot, ClaudeBot, Google-Extended all allowed

## Open, smallest first

1. ~~**Organization schema.**~~ DONE 2026-08-29. Added to all 12 pages, `@id`
   `https://yaelfried.com/#organization`, with logo, sameAs, founder, areaServed,
   availableLanguage. All JSON-LD on all 12 pages re-parsed clean after the edit.
2. ~~**A real link to breaktheloop.**~~ DONE 2026-08-29, both directions verified.
   yaelfried.com had none, now has a footer link on all 12 pages after the copyright line,
   in each page's own language (commit `4a5902e`). breaktheloop ALREADY linked back to
   yaelfried.com, one plain anchor, no nofollow, checked live. The July audit's "zero links
   either way" was half wrong.
   Note on where breaktheloop lives: it is NOT `C:\Projects\Landing_Page`, that folder holds
   only `noindex` redirect stubs pointing at the live subdomain. The real source is not on this
   machine. `C:\Projects\The_Way_Back_To_Yourself` is a different app (`the-journey-to-yourself`).

3. ~~**Duplicate h1 per page.**~~ NOT A REAL ISSUE, closed 2026-08-29. Framer emits the same
   heading once per breakpoint with identical text. One distinct h1 per page. Nothing to fix.
   The homepage h1 keeping the loop phrase is also correct and deliberate: the cannibalization
   rule in [[project-yaelfried-site-title]] covers `<title>`/`og:title` only, not the on-page h1.

4. ~~**FAQPage on the trance page.**~~ DONE 2026-08-29, commit `c5a424d`. Three answered
   questions marked up on each language version, answers taken verbatim from the page copy,
   Framer's per-breakpoint sentence duplication collapsed first.
5. **Split the treatment page.** One page currently carries ten conditions. Biggest job here,
   and the one with the most upside for AI answers, since each condition becomes its own
   citable page. Audit priority 5.

## Constraints that bind any GEO copy

- Hypnosis (היפנוזה) is restricted in Israel. It may name her training, never a service offered.
  Public-facing copy says טראנס וסוגסטיה on purpose. Do not reintroduce it for keyword reasons.
- The לצאת ממעגל התקיעות phrase belongs to breaktheloop. Keeping it off the homepage is deliberate,
  so the two properties stop cannibalising each other.
- Hebrew pages stay `dir="ltr"`. Setting `dir="rtl"` on the root flips every Framer flex row.

## Known measurement gap

Search Console is still not connected. Without it, the keyword targeting in the 12 titles is
reasoning rather than measurement. It is the one input that would change decisions instead of
confirming them.
